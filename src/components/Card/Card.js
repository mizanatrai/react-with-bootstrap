import React from 'react';

const Card = ({product}) => {
    const {name, id, price} = product;
    return (
        <div className="card">
            
        </div>
    );
};

export default Card;